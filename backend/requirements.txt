Flask==3.0.2
voluptuous==0.12.2
stringcase==1.2.0
SQLAlchemy==2.0.29
pytz==2022.1
PyGithub==1.55
pycryptodome==3.21.0
aiohttp==3.9.4
redis==5.0.3
python-redis-lock==4.0.0
psycopg2==2.9.3
python-dotenv==1.0.1
gunicorn==22.0.0
Flask-SQLAlchemy==3.1.1
# CLUSTOX: mhq/exapi/jenkins.py imports requests directly. It resolved only
# transitively through PyGithub before, so an upstream bump that dropped it
# would have broken the Jenkins client.
requests==2.32.5
# CLUSTOX: mhq/exapi/jenkins.py enforces its wall-clock ceiling with
# response.raw.read1(), which urllib3 only grew in 2.0. requests itself accepts
# anything <3, so without this pin an environment could resolve urllib3 1.26
# and the Jenkins client would fail with AttributeError on every call.
urllib3>=2.0,<3
